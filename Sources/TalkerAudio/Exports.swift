// Re-export StreamAudio so importing TalkerAudio is enough to use
// StreamAudioPlayer (e.g. realtime raw PCM playback) without adding a
// direct package product dependency.
@_exported import StreamAudio
